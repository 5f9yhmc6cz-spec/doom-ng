# DOOM-NG VSLICE debug params

Generated 2026-06-22 by the param-audit workflow (read + verified vs neogeo/main.c). In debug (SPACE), WASD grid-nav, N/M adjust, P toggles BSP-walk viz.

Cost legend: **WALK**=BSP node-box tests (bx) · **EMIT**=SCB strip writes (em) · **FLATS**=vs_lut (fl) · **PROJ**=per-seg · **none**=cosmetic.

| # | name | definition | values / default | cost | effect | interactions |
|--|--|--|--|--|--|--|
| 0 | `dd` | Wall draw distance: index into DD[] (world units), assigned to g_vs_murk and resolved to the effective far-horizon g_murk_eff. Walls beyond are not drawn; floor/ceiling LUT + backdrop carry the gap. HUD shows the resolved DD[] value (g_vs_murk), not the index. | DD[] has 33 entries (VS_NDD=33): {0,100,150,200,250,300,350,400,450,500,550,600,650,700,750,800,850,900,950,1000,1500,2000...32000}. case 0 wraps the index +/-1. Default g_dd=14 -> DD[14]=750. DD[0]=0 = fog-only; DD[32]=32000 = whole map. | WALK + EMIT + PROJECTION. The biggest single perf lever. Lowering dd prunes the BSP walk via the node far-cull (vs_bbox_vis line 1063: g_ncull && dmin>g_murk_eff -> skip subtree) and the per-vertex cull (line 1046), and gates per-strip emit (line 977 dC<=g_murk_eff). Lower = fewer nodes visited, fewer segs projected, fewer strips emitted = faster. Only actually prunes the WALK when ncul(35) is on; otherwise it only suppresses emit. | How far you can see walls. Low = a near fog wall (floor/ceiling/backdrop beyond); high = the whole level draws. Below ~450 the murk/backdrop is visibly close. | Core of the dd<->mn<->ease triad: dd is the HARD far ceiling (g_murk_eff is clamped to g_vs_murk LAST, line 1293, so dd always bites even when mmin>dd). ease(6) governs how g_murk_eff drifts between mmin and dd under budget pressure; mmin(8) is the floor it can pull to. ncul(35) gates whether dd prunes the walk vs only emit. rad(23) reinterprets dd as a per-column radial reach. Actors cull at min(g_murk_eff,dd) (line 1315) and add(41) is the independent actor-dd. Driven by the perf presets pf(26) and reset by rset(27, ->dd750). |
| 1 | `dc` | Per-column see-through DEPTH CAP: index into DC[], assigned to g_dcap. Caps how many overlapping wall strips (layers) a single screen column may stack before the column fogs out and closes. HUD shows the resolved DC[] value (g_dcap), not the index. | DC[VS_NDC=11] = {1,2,3,4,5,6,7,8,16,32,48}. case 1 wraps +/-1. Default g_dci=6 -> DC[6]=7. 48 ~= effectively unlimited. (rset sets g_dci=6.) | EMIT. Read per emitted strip at line 1032 (vs_nstr[c]>=g_dcap -> close the column, set vs_wdep, decrement vs_open). Lower cap = columns close sooner in deep overlapping views -> fewer strips written + the walk ends earlier (vs_open hits 0). Lowering speeds dense see-through scenes; in simple rooms it does nothing (few layers). The global strip budget (bud) is the real bound; this is a secondary per-column lid. | Controls how many transparent/overlapping wall layers stay visible down a corridor before it fogs out. Low = the corridor fogs out after a few layers (depth foreshortened); high = you see through many overlapping wall edges into the distance. | Subordinate to bud(13) (the global budget is the dominant limiter). Interacts with dd/mmin: a column that hits g_dcap also writes vs_wdep, which OCCLUDES far actors (occl, 12) behind the fogged column. Driven by pf(26) presets and rset(27). |
| 2 | `col` | COLUMN RESOLUTION: index into NCOLW[], selecting {ncol, colw, colrcp}. Trades wall-strip width for serration; ncol*colw=320 always. colw is the HW h-shrink strip width; colrcp is the screen-x->column reciprocal. HUD shows NCOLW[].name+3 (the digits after 'col'). | NCOLW[NNCOL=6] = col20(20x16), col32(32x10), col40(40x8), col64(64x5), col80(80x4), col160(160x2). case 2 wraps +/-1 and plays SND(1). Default g_ncoli=0 = col20. col80 = HW sprite dropout rung; col160 = over-spec (2px, exceeds ~96/scanline limit on purpose). | EMIT (dominant) + PROJECTION + FLATS. Higher ncol = more wall strips per frame (more SCB writes), more per-column projection work, and the LUT/flat span loops run over more columns. col20 is the leanest; col80/col160 push past the per-scanline HW sprite limit. This is the second-biggest perf knob after dd/bud. Lower ncol = faster. | Wall sharpness vs serration: low col = wide 16px strips (blockier walls, fewer vertical seams); high col = thin strips (smoother walls but more visible seams/flicker and HW dropout at col80+). | Recomputes g_cosrad on change (line 1263, col-res change only). seam(15) only bites at col>=32 (col20 strips are already max 16px width). All the colrcp screen->col maps (flats, occl, sky-block) depend on it. Driven by pf(26) presets (lo/md=col20, hi=col32, ul=col40) and rset(27, ->col20). |
| 3 | `cap` | WALL CAP MODE: index into CAPMODE[], selecting the diagonal-bevel treatment for sloped wall top/bottom edges (per-edge slope gate tg/bg + raise mode tr/br). Smooths the staircase serration on angled wall edges. HUD shows CAPMODE[].name truncated to 4 chars. | CAPMODE[NCAPMODE=24]: idx0='off', idx1='TB32f' (flat, picket-proof), idx2='TB32g' (grid-lock), idx3='TB32o' (opaque-corner overlay sprite), plus many T*/B*/asymmetric variants. case 3 wraps +/-1 and re-uploads cap palettes if CAP_OPAQUE (TB32o). Default g_capmode=0='off' (cap quantization made diagonals snap frame-to-frame). rset sets 0. | PROJECTION + EMIT. When g_capmode!=0, vs_render_seg computes 4 extra per-seg slope divides (dyt/dyb/dytO/dybO, lines 955-957) that are SKIPPED entirely when off (line 955 gate). The cap edges also draw extra cap tiles (and TB32o draws an extra overlay HW sprite per capped corner, budget-backstopped). Off is bit-for-bit lean; any non-off mode adds per-seg divides + per-strip cap work. Off = fastest. | Smooths the jagged stairstep on diagonal wall tops/bottoms. 'off' = raw staircase. Flat modes (f) cut the bevel INTO the wall (never protrude = picket-proof); raise modes (r) chain the diagonal across columns (smooth but convex corners 'picket'); TB32o overlays an opaque corner sprite that merges with the floor/ceiling. | Gates its own per-seg divide cost (the only param that adds projection-stage divides). TB32o (CAP_OPAQUE) reserves palette slots 242/243 and drops the wall/flat allocator ceiling by 2 (vs_upload_cap_pals re-asserted on select). Independent of dd/bud but competes for the strip/sprite budget. |
| 4 | `zon` | ZONAL flats: per-(block,row) visplane stamp so each depth band gets the correct floor/ceiling flat instead of a single blanket flat. 4 states: off / zonal / zonal+bias-up / zonal+bias-down. Only meaningful when gen=0 (real flats). | 0..3 (case 4 = (g_zonal+1)%4, no direction). 0=off (single-flat blanket), 1=zonal, 2=zonal+bias GREY-into-pit (boundary leans up), 3=zonal+bias PIT-into-grey (boundary leans down). Default g_zonal=0 (off until full-flats pack hardened). rset sets 0. | FLATS (vs_lut + per-seg stamp). When zon&&!gen the renderer does a per-column VISPLANE STAMP during the seg walk (line 994, block-indexed) AND the LUT does a per-(block,row) flat resolve via vs_flatres (lines 851/884). gen=1 short-circuits both (the stamp is pure wasted memory-bound work without it -> that's the perf win of off/gen). Off (or gen=1) = faster; on adds the stamp write per column + the per-block flat lookups. | Correct floor/ceiling texture per depth region (e.g. a nukage pit reads blue, the main floor grey, at the right boundary) instead of one flat smeared over everything. bias 2/3 shift the contested main/pit boundary by one 16px row in opposite directions to A/B the lean. | Gated entirely by gen(5): zon does nothing while gen=1 (synthetic blanket). Pairs with flod(11)/clod(16) (which rows of the zonal LUT draw) and fdbg(32) (visualises the per-flat source colour). lutcull(29) parks fully-hidden zonal blocks. fmrk/cmrk(18/19) only affect the SYNTHETIC planes (gen=1), not zonal. |
| 5 | `gen` | GENERIC mode: force the synthetic floor/ceiling/sky LUT (the pre-real-flats look) instead of reading real WAD flats. A master A/B between the synthetic planes and the textured-flat pipeline. | 0 or 1 (case 5 = !g_generic). Default g_generic=1 (synthetic). rset sets 1. | FLATS. gen=1 is the CHEAPER path: it skips the real-flat resolve (fbase/cbase stay synthetic, line 836) and skips the zonal visplane stamp (line 994). gen=0 enables real-flat lookups (vs_flatres per block/row) and, with zon, the per-column stamp. gen=1 = faster (it's the lean default for exactly this reason). | gen=1 = the synthetic gradient floor/ceiling + procedural sky (uniform per-band look). gen=0 = real WAD flat textures on floors/ceilings (more authentic, more detailed). | Master gate for zon(4) (zonal is a no-op while gen=1), and for fdbg(32) and the real-flat path. fmrk(18)/cmrk(19) far-row murk applies ONLY to the synthetic planes (gen=1). flod(11)/clod(16) apply to both. rset returns to gen=1. |
| 6 | `ease` | Far-cull EASING gain, expressed as a bit-shift: the per-frame rate at which g_murk_eff drifts toward mmin (under budget pressure) or back toward dd (with slack). 0=off (constant murk, no horizon jump); higher shift = gentler/slower drift. | 0..16 in COARSE steps of 4 (case 6: g_murkease += dd*4, clamped 0..16). 0=off, 4=moderate, >=12 ~imperceptible (the per-frame step floors at 1 unit so 12-16 are visually identical). Default g_murkease=16 (set by rset; the declared init is also 16). NEASE=65 but the dial only spans 0..16. | NONE (governor only). Used once per frame in vs_render's easing block (lines 1289-1293): a shift on (g_murk_eff-mn) or (g_vs_murk-g_murk_eff). No hot-path (per-seg/per-strip) cost. It only changes how SMOOTHLY the far-cull moves, not how much work each frame does. Cosmetic/temporal. | Controls the visible drift of the fog horizon when the frame exceeds budget. ease=0 = the horizon snaps/jumps as load changes; high ease = the horizon drifts in/out gently (no jump). Only visible when frames exceed bud. | Pure middle term of dd<->mmin<->ease: it interpolates g_murk_eff between mmin(8, the floor) and dd(0, the ceiling) based on whether 'used' is near g_vs_budget (line 1291) or has slack (1292). Inert if mmin=OFF or dd makes the range zero. No effect unless the frame is budget-pressured. |
| 7 | `wpn` | WEAPON select: which of the 6 first-person weapon sprites (fist/pistol/shotgun/chaingun/rocket/chainsaw) is drawn in the gunhand fix layer. A debug/preview cycle, not gameplay-bound. | 0..GUNHAND_N-1 (case 7 wraps +/-1). 0=fist .. 5=chainsaw. Default g_weapon=1 (pistol). g_pweap tracks last-drawn for change-only redraw. Not touched by rset. | NONE (cosmetic, fix layer). Only redraws the weapon sprite in the fix/HUD layer when it changes (g_pweap!=g_weapon). No 3D-renderer (walk/emit/flats) cost at all. | Swaps the weapon shown at the bottom of the screen. | Independent of the renderer dials. Related only to cln(20)/hhud(21) which can hide the weapon+HUD (hidegun). Not preset-driven, not reset. |
| 8 | `mn` | MURK FLOOR (mmin): the minimum the eased far-horizon g_murk_eff may pull IN to under budget pressure. Index into MURKMIN[]. Sets how aggressively the far-cull can shorten the view to claw back frame time. HUD label is 'mn' (truncated from mmin); shows the MURKMIN[] value or 'of'. | MURKMIN[NMURKMIN=30] = {0,50,100,150,200,250,300,350,400,450,500...1000,1500...5000,-1}. case 8 wraps +/-1. Last entry (-1) = OFF (far-cull disabled, draw to the strip budget). Default g_murkmin=5 -> MURKMIN[5]=250 (the comment '250' matches; rset sets 5). pf presets set it (lo=200, md=500, hi=800, ul=off). | WALK (indirectly, the big one). mn is the floor that ease pulls g_murk_eff down to (lines 1287-1293). A LOW mn lets g_murk_eff shrink far under load, which then prunes the BSP walk via the node far-cull (ncul) and gates emit -> the actual perf recovery. mn=OFF means no easing pull-in (g_murk_eff=g_vs_murk constant, line 1288) so dd alone clamps. Lower mn = more walk pruning available under pressure = faster recovery. | How short the view is allowed to get when the engine is struggling. Low mn = the fog can close right in (heavy near-fog) to protect frame rate; OFF = the view never shortens for perf (dd is the only limit), risking slowdown in dense scenes. | The FLOOR term of dd<->mmin<->ease. Clamp order (line 1293): floor mn first, then dd ceiling LAST, so dd always wins even if mn>dd. Inert unless ease>0 AND the frame is budget-pressured (else g_murk_eff=dd). Works WITH ncul(35) to convert the pull-in into actual walk savings. Driven by pf(26), reset by rset(27). |
| 9 | `mbg` | MURK BACKDROP: the far-field colour painted in undrawn regions (the furthest murk layer / backdrop palette entry). Index into MURKBG[]; applied immediately by writing the backdrop palette slot. | MURKBG[NMURKBG=8] = {0x0000,0x0111,0x0222,0x0333,0x0444,0x0555,0x0210,0x0421} (xRGB). case 9 wraps +/-1 and writes MMAP_PALBANK1[4095]=MURKBG[g_murkbg] now. Default g_murkbg=1 = 0x0111 near-black (deepest murk). rset forces g_murkbg=1. | NONE (one palette write on change). Cosmetic. The backdrop fills regions nothing was drawn into; no per-frame renderer cost. | The colour of the deep distance / void where no walls or floor/ceiling LUT reach. Near-black (default) reads as deep murk; brighter values lift the far horizon. | Conceptually the far end of the fog gradient that the wall fog bands (fog, 10) and floor/ceiling murk (fmrk/cmrk) cull toward. Purely cosmetic; no interaction with the perf dials. rset resets it to idx1. |
| 10 | `fog` | Wall fog-band EXTENT %: scales the two depth thresholds (g_fog0/g_fog1) that pick a wall's shade band (near/mid/far darkening). Index into FOGEXT[]. HUD shows the FOGEXT[] percent or 'of'. | FOGEXT[NFOGEXT=16] = {5,10,15,20,25,30,35,40,45,50,55,60,65,70,75,0} percent. case 10 wraps +/-1 and recomputes: if 0 -> g_fog0=g_fog1=32767 (OFF); else g_fog0=400*pct/100, g_fog1=1000*pct/100. Default g_fogext=14 -> 75%. Last (0) = OFF (no wall fog). rset sets 14. | Near-NONE in the hot path. Per-strip the renderer does one cheap band classify (line 654: plvl = d<g_fog0?0:(d<g_fog1?1:2), clamped to the map's adaptive band count). That comparison happens regardless; fog only moves the thresholds. The recompute is once per dial change. Effectively cosmetic for perf. | How near the camera walls start darkening with distance. LOW % = fog onset near the camera (heavy near fog); 75% = fog pushed back (light); OFF = walls keep full brightness to the draw distance (no depth shading). | Shade only; orthogonal to dd (the hard cull) and mbg (the backdrop colour) though they read as one gradient. Clamped to the map's adaptive g_nfogband count (dense maps run 1-2 bands). rset restores 75%. |
| 11 | `flod` | FLOOR crop / LOD: how many far (horizon-side) rows of the floor LUT are dropped. Index into FCLOD_F[] giving g_floor_rows (rows drawn, keeping NEAR=bottom). Independent of the ceiling crop (clod, 16). | 0..NFCLOD-1 (NFCLOD=10; case 11 wraps +/-1). FCLOD_F[]={5,5,4,4,3,3,2,2,1,0}: idx0=full(5 rows) .. idx9=off(0 rows, floor parked). Default g_floorlod=0 -> 5 rows (full floor; rset sets 0). pf presets crop it (lo=6,md=3,hi/ul=0). | FLATS (vs_lut). g_floor_rows is the row count of the floor LUT draw loop (lines 828, 880: if(g_floor_rows>0 && fhi>fskip && !floorcov)). Fewer rows = fewer floor LUT span-writes per column block = faster. Dropping all rows (idx9) parks the floor entirely. This is a direct FLATS-emit lever. Lower row count = faster. | How far the textured/synthetic FLOOR extends toward the horizon. Full = floor reaches the centre horizon; cropped = the floor recedes from the horizon outward, leaving murk/backdrop beyond the near rows; off = no floor drawn. | Twin of clod(16) (ceiling crop) - independent now (was combined). Both feed the bench harness (it forces 4/6 rows for the flat-cost probe, lines 1819-1822). lutcull(29) and the floorcov check can also park rows. Works on both gen and real-flat paths. pf(26) presets set it; rset(27) ->0 (full). |
| 12 | `occl` | ACTOR OCCLUSION vs walls (M2): hide actors (barrels/monsters/props) whose every drawn column sits behind a nearer SOLID wall (per-column, using vs_wdep with VS_RAD fuzz). 0=draw actors over everything. | 0 or 1 (case 12 = !g_occl). Default g_occl=1 (on). rset sets 1. | Actor-stage (minor, NOT the wall hot path). When on, vs_render does a per-actor occlusion pre-cull (line 1329: sample the render-cols the actor spans, skip if all behind nearer vs_wdep) BEFORE atan2 + vs_billboard, and vs_billboard itself rechecks per-column (line 1160). The pre-cull SAVES work (skips billboard for fully-hidden actors), so on is often net-neutral-to-faster; off draws every actor unconditionally. Scales with actor count, not wall count. | Whether monsters/barrels correctly disappear behind walls. on = a barrel behind a pillar is hidden; off = it draws on top of the wall (drawn through the wall). Per-column can over-cull an actor near a wall edge (the A/B reason). | Depends on vs_wdep being set, which DC(1)/dcap (fogged columns) and budget-halted walls also write (the budget-halt leak fix, line 1316, caps actors at the deepest drawn wall). Interacts with bud(13): if the walk halts early, far walls don't set vs_wdep so occl has less to test against. props(17) hides actors entirely (occl moot then). maxact(42)/add(41) bound which actors are considered. |
| 13 | `bud` | DRAW-COUNT CAP (strip budget): the nearest-N wall strips drawn per frame. Index into BUDGET[] -> g_vs_budget. The primary flicker/perf lever bounding the HW sprite count (41 fix + bud walls + actors < 380). | BUDGET[NBUDGET=27] = {24,36,48...288,300,312,324,336} (steps of 12). case 13 wraps +/-1. <=288 = HW-safe; 300-336 = over-spec (the SCB-safety cap makes excess graceful). Default g_budgeti=26 -> BUDGET[26]=336 (over-spec; perf mature enough). pf presets set it (lo=48,md=96,hi=192,ul=276). rset sets 26. | EMIT + WALK (the dominant emit lever). With dpri OFF: emit halts at g_vs_budget in walk order (lines 897, 977) and sets vs_open=0 to STOP the walk -> a hard near-strips-only cap (faster, but far walls can drop in walk order = flicker). With dpri ON: the walk records up to the full buffer (VS_SBUFN) then a post-pass (lines 1296-1300, a 64-bucket depth histogram) keeps the nearest g_vs_budget by DEPTH. Lower bud = fewer strips + earlier walk halt = faster. The single most direct EMIT throttle. | How many wall strips can be on screen at once. Low = sparse, near-walls-only in dense rooms (far geometry drops, possible flicker); high = crowded, full scenes (risks HW sprite dropout at >288 squeezing actors). | Behaviour mode set by dpri(40): dpri changes bud from a walk-order halt into a depth-prioritised keep-nearest (so near walls never drop to BSP visit order). dc(1)/dcap is a per-column sub-cap under it. The budget-halt also bounds far actors (line 1316). ease(6) reacts to 'used' vs g_vs_budget-16/-48 (lines 1291-1292). col(2) raises strip demand against the same budget. pf(26)/rset(27) set it. |
| 14 | `sky` | SKY-IN-OPENING: draw sky through windows/openings whose BACK ceiling is the sky flat (#42) while the front is not (i.e. indoors looking out). Suppresses the from-outside-into-an-indoor-window case. | 0 or 1 (case 14 = !g_opensky). Default g_opensky=1 (on). rset sets 1. | EMIT (minor). When on, an opening whose back ceiling is sky emits an extra even sky strip per 16px block (line 1026, deduped per block via vs_skyblk so it's one strip per block, not per wall column). on adds a small number of sky strips; off skips them. Low impact relative to bud. | Whether you see sky through a window/door opening into an outdoor sector. on = sky shows through the opening; off = the opening's ceiling stays room ceiling / backdrop (no see-through sky). | Depends on SKY_TEX>=0 and the back/front sky-flag test (fl&4 && !(fl&2)). Deduped via vs_skyblk (block-indexed like the flats). Conceptually pairs with the ceiling LUT/backdrop. Not preset-driven beyond rset(27, ->on); independent of the perf dials. |
| 15 | `seam` | SEAM OVERDRAW: widens each wall strip N px to the RIGHT (SCB2 hshrink, line 725: hsh=g_colw-1+g_seamover, clamped <=15) so adjacent strips OVERLAP, masking a column that is mid-rebuild with its neighbour's pixels instead of showing a blank gap (the temporal-parallax flicker mask). | 0..3 px (case 15: +4+dd &3, 4 steps). Default 0 (off). Only bites at col>=32; at col20 strips are already 16px (=max hshrink) so seam is a no-op there. | EMIT, but free: the only touch is one extra arithmetic in the per-strip SCB2 write (hsh clamp at line 725). No extra strips, no extra walk. Negligible perf either direction. | Wall strips bleed rightward over their neighbour -> kills thin black vertical seams/flicker between strips during movement, at the price of slightly fatter, less-crisp strip edges. | Coupled to col (param 1/g_colw): only matters when colw<16 (col32/64/80); inert at col20. Preset-driven (set from P->seam at line 1578). Cosmetic companion to the flicker family (bud/mmin/ease/hwt stabilise the far-cull; seam masks the near-strip rebuild gap). |
| 16 | `clod` | CEILING crop LOD: number of FAR (horizon-side) ceiling rows DROPPED from the floor/ceiling LUT, keeping the TOP. Index into FCLOD_C[]={7,6,6,5,5,4,4,3,2,0}; applied at line 1788 g_ceil_rows=FCLOD_C[g_ceillod], consumed in vs_lut as 'crow' (line 829) -> how many ceiling tile-rows the LUT draws. | 0..9 (case 16: +NFCLOD+dd %10). 0=fewest rows (7 dropped, deepest crop), 9=full ceiling (0 dropped). Default 0 at boot decl; reset (case 27) sets 0; preset-driven via P->clod (lo=6/md=3/hi=0/ul=0). | FLATS (vs_lut): fewer ceiling rows = fewer per-block SCB rows written in the ceiling loop (lines 842-867). Cropping MORE (lower index) speeds up the LUT stage. Independent of WALK/EMIT. | Recedes the ceiling from the horizon (centre) outward -> the far ceiling fades to backdrop earlier. Lower value = more ceiling missing near the horizon line. | Twin of flod (param 11, floor crop) -- they were one combined dial, now independent. Both feed vs_lut. Preset-driven (case 26 sets clod from PERF[]). cmrk (param 19) darkens the bottom g_vph of whatever rows clod leaves; cskip (letterbox g_vpt) also trims the top. |
| 17 | `prop` | PROPS visible: master toggle for drawing map THINGS (barrels, monsters, item billboards). g_props=1 draws the whole depth-sorted actor pass (line 1312 block); g_props=0 skips it entirely for geometry-only A/B debug. Also gates item-pickup (case at line 1697, 1321). | Boolean 0/1 (case 17: g_props=!g_props). Default 1 (on). | EMIT + projection of actors. OFF skips the entire actor block: the per-thing box reject, depth MUL + insertion sort over ve_nth, atan2 rotation, occlusion pre-cull, and every vs_billboard SCB write. Turning it OFF is a large speedup in thing-dense rooms; the wall WALK/LUT are untouched. | Hides all monsters, barrels, item billboards, and the barrel-explosion FX. Pure geometry view (walls + flats + sky only). | Gates the whole actor family: add (param 41, DD[g_actddi] actor draw-dist), mxa (param 42, g_maxact nearest-N), occl (param 12, actor occlusion pre-cull), rad/dd/bud (actors cull at acull = min of murk/draw-dist/budget-halt depth). With prop=0 none of those matter. Also disables item pickup + barrel combat. |
| 18 | `fmrk` | FLOOR MURK: number of FAR (horizon-side) floor rows in the synthetic-plane LUT darkened to a 3-band murk gradient toward the backdrop. g_vpw; consumed in vs_lut floor loop (line 883): far g_vpw rows ramp pal 11(0.90x)->10(0.78x)->14(0.45x deep). Repurposed from the ditched viewport-crop variable. | 0..5 rows (case 18: +NVP+dd %6, NVP=6). Default 0 (off). Reset (case 27) sets 0. | FLATS (vs_lut), trivial: just changes the per-row palette pick inside the floor loop that already runs; no extra rows/strips. Effectively NONE perf-wise. | Fades the far floor into distance fog (3 darkening bands at the horizon). Crank to 3..5 to see all three bands. Note: only applies to the SYNTHETIC floor plane (gen=1, the two synthetic planes), not real flats. | Floor twin of cmrk (param 19, ceiling murk g_vph). Effective only when gen (param 5) selects the synthetic plane. Stacks visually with the overall fog/murk distance system (fog param 10, dd/murk far-cull) but is a separate near-horizon shade. Bounded by the floor rows that flod (param 11) leaves drawn. |
| 19 | `cmrk` | CEILING MURK: number of FAR (horizon-side) ceiling rows darkened to a 3-band murk gradient. g_vph; consumed in vs_lut ceiling loop (line 850): bottom g_vph rows ramp pal 9(0.70x)->15(0.55x)->14(0.45x deep)->backdrop. Repurposed from the ditched viewport-crop variable. | 0..5 rows (case 19: +NVP+dd %6, NVP=6). Default 0 (off). Reset (case 27) sets 0. | FLATS (vs_lut), trivial: changes the per-row palette pick inside the ceiling loop that already runs; no extra rows. Effectively NONE. | Fades the far ceiling into distance fog (3 darkening bands toward the horizon). Crank to 3..5 to reveal all three bands. Synthetic ceiling plane only (gen=1). | Ceiling twin of fmrk (param 18, floor murk g_vpw). Effective only with gen (param 5). Bounded by the ceiling rows clod (param 16) leaves; cskip/letterbox trims from the other end. Visually layers with the global fog (param 10) / far-cull (dd/murk). |
| 20 | `cln` | CLEAN VIEW: single toggle that hides BOTH the first-person weapon and the player status bar (case 20 sets g_hidehud=g_hidegun=!g_hidegun -- the merged hgun+hhud toggle). HUD label reads cln=g_hidegun. g_hidegun gates draw_gun (line 784 returns) + muzzle flash (line 1363); g_hidehud gates the status-bar/face redraw (lines 1803-1806). | Boolean 0/1 (case 20: !g_hidegun, mirrored into g_hidehud). Default 0 (both shown). | NONE on the render hot path -- gun + HUD are FIX-layer, not SCB sprites. Hiding them just stops two fix-map redraws (gun band clear; status-bar tile blank on toggle). No WALK/EMIT/FLATS impact. | Clears the weapon sprite AND the bottom status bar -> unobstructed full-frame view of the geometry (the screenshot/clean-look toggle). | Drives both off-grid params it merged: hidegun (was param 20-gun) and hhud (param 21, g_hidehud). Toggling cln overrides whatever hhud was set to. hhud (case 21) can still flip the HUD alone afterward. Pairs with prop=0 (param 17) for a totally bare geometry capture. |
| 21 | `hhud` | HIDE HUD only: toggles just the player status bar (g_hidehud), independent of the weapon. Off-grid now (kept for completeness; cln/param 20 is the primary control). On toggle (line 1803) it blanks the 40-col x 4-row HUD fix-map region once, or redraws the bar + forces a face repaint. | Boolean 0/1 (case 21: !g_hidehud). Default 0 (shown). Off-grid: not in the P-cycle grid, only reachable directly. | NONE on the render path -- status bar is the FIX layer, redrawn once on toggle, not per frame. No WALK/EMIT/FLATS cost. | Hides/shows the bottom status bar (ammo, face, STARMS, health) without touching the weapon sprite. Un-hide forces the face animation to repaint (g_faceN=-1). | Subset of cln (param 20), which sets both g_hidehud and g_hidegun together -- toggling cln will clobber an independent hhud state. The per-toggle redraw also re-runs draw_face (line 1806 gated on !g_hidehud). |
| 21 | `hhud` | Off-grid hide-HUD-only toggle: hides just the player status bar (not the weapon), kept for completeness after param 20 (cln) merged hidegun+hidehud. Sets g_hidehud (line 405). On a change (g_hidehud != g_phidehud, line 1803) the static status bar is cleared/redrawn once: when hidden it blanks the fix-layer HUD row (line 1804), and when shown draw_face runs (line 1806). | Boolean toggle 0/1. Default 0 (shown). Case 21 toggles g_hidehud only (no forced redraw; the once-on-toggle clear at line 1803 handles the fix layer). | NONE for the render hot path - the status bar is fix-layer, cleared/redrawn ONCE on toggle (line 1804), not per frame. No effect on WALK/EMIT/FLATS/PROJECTION. Hiding it does free the bottom render row conceptually but the toggle itself is cosmetic. | Shows/hides the bottom player status bar (ammo/health/face). The 3D view and weapon are unaffected (unlike cln/param 20 which hides both). | Overlapped by param 20 (cln), which sets BOTH g_hidegun and g_hidehud in one toggle - cln is the merged clean-view; hhud is the status-bar-only remnant. The face idle look-around (draw_face, line 1806) only runs when !g_hidehud. Reset (case 27) does not explicitly set g_hidehud. |
| 22 | `dwlk` | DOORS walk-through: when on, the movement collision test treats locked/closed doors as passable (vs_move_ok line 1379 'if(g_doorwalk) continue' before the open-gap check) -- you walk through doors without triggering/waiting for them to open. | Boolean 0/1 (case 22: !g_doorwalk). Default 1 (ON -- unobstructed movement while the trigger system settles). | NONE on render. It short-circuits one branch in vs_move_ok (the collision sweep, called on movement), actually saving the door open-gap arithmetic. Not in vs_render/vs_lut at all. | No visual change to the frame. Behavioural: player passes through closed doors instead of being blocked; doors don't need to be opened to traverse. | Independent of the render dials. Interacts only with the door/lift state machine (g_doorstate, g_secdc) and the static-skip (door mid-cycle forces redraw, line 1798) -- but dwlk bypasses opening doors, so with it ON doors stay at rest and don't churn the geometry. |
| 23 | `rad` | RADIAL far-cull STRENGTH curve. Two effects: (1) per-seg in vs_render_seg (line 906) skips segs WHOLLY beyond the EUCLIDEAN far-cull (R2 = murk^2) before projection; (2) per-column in vs_render (line 976) CLOSES a column whose nearest wall is beyond the radial reach g_murkcol[c]. Strength lerps the per-column reach from flat slab (256) to full cos pull-in (line 1264, bcos blend). | 0..8 (case 23: +NRAD+dd %9, NRAD=9). 0=OFF (flat perpendicular slab, dC<=g_murk_eff). 1=barely pulls eccentric edges in, 8=full euclidean radial cone. Default 0 (off). Reset (case 27) sets 0. | Mixed. The per-seg test (line 906) costs 4 MULs on EVERY seg but only culls the wholly-far minority -> the code comment flags it net-SLOWER in isolation. The WIN is the per-column CLOSE (line 976): ending the walk early at screen edges shrinks the WALK + EMIT. Also pays a per-column cos refresh each frame (line 1264) only when rad>0. Higher strength = more edge columns closed = faster walk, narrower draw at edges. | Carves the flat far-cull slab into a uniform-radius cone sector: screen-edge geometry culls SOONER (the edges no longer reach ~1/cos farther in true distance). Higher rad = harder edge pull-in, less peripheral draw distance. | Scales off dd/murk (g_murk_eff is the radius; rad bends it per-column via g_cosrad/g_murkcol). Depends on col (g_ncol) for the per-column cos table. Floor/ceiling LUT is independent of the column-close (vs_ct/vs_cb), so near flats still draw + fade past a radial-closed wall column. Trades edge draw-distance for walk savings smoothly (vs the all-or-nothing dd). |
| 24 | `vmap` | VERTICAL MAP mode: how wall textures map vertically. 0=ORIGINAL stretch (whole texture to wall height, no tiling/peg); 1=V companion everywhere (1:1 world-scale + vertical tiling + DOOM peg, AFFINE depth); 2=SELECTIVE (companion for single-copy posters, stretch for tiling walls); 3=companion everywhere + PERSPECTIVE-CORRECT depth (1/z-linear wf, line 983 feeds dpix) -> level tiling on angled walls. Selected in vs_render_seg per-seg (line 707) + the wf depth (line 980-983). | 0..3 (case 24: +1 %4, forces g_redraw=1). Default 3 (decl line 493 -- the comment's 'default 2' is stale). NOT preset-driven (line 1578 note: stays at toggle/init/reset). Reset (case 27) sets 3. | PROJECTION (per-seg). Mode 3 adds the 1/z-linear world-fraction interp -> ~1 divide per drawn seg (shared den at line 980, gated by g_hmap==3\|\|g_vmap==3). Modes 0/1/2 are affine (no divide). Mode 0 is cheapest; mode 3 costs the perspective divide. Toggling forces a full redraw. | Mode 0: textures stretch to fill wall height (squashed/unsquashed by wall size). 1/2/3: textures sit at true 1:1 world scale, tile vertically on tall walls, DOOM-pegged. Mode 3 additionally keeps brick/panel courses level on angled walls (kills affine vertical swim). | Twin of hmap (param 38, horizontal-U): vmap=3 and hmap=3 SHARE the perspective-divide (the wf 1/z computed once at line 980 when EITHER is 3 -> mode 3 on both is nearly the cost of one). Mode 2 (selective) keys off the same isposter test as hmap=2 (ve_ulen<=TEXWT, line 923). voff DOOM-peg only applies in companion modes. |
| 25 | `lvl` | LEVEL indicator / map selector. HUD label lvl=g_map+1 (1-based map number). The tk[25] line is the display; case 25 is NOT in the param switch (no in-place +/- handler shown in 1748-1767) -- map switching is driven elsewhere (g_map), this is the read-out. | Display of g_map+1 (1..9 for E1, more for whole-episode builds). Default = the boot map (g_map). No B/N adjust case in the documented switch range. | NONE -- pure HUD read-out. (Actually changing g_map triggers a full per-map reload: geometry headers + palette upload, but that is the map-load path, not a per-frame dial.) | Shows which map is loaded. Changing the underlying g_map swaps the entire level (geometry, flats, palette, things). | Reflects g_map, the whole-episode build's per-map state. A map change re-bakes per-seg pegging and re-uploads the per-map palette (THE constraint per the memory notes), and resets g_redraw. Not coupled to the render-tuning dials. |
| 26 | `pf` | PERF PRESET selector: cycles the lo/md/hi/ul dial bundle. case 26 (line 1763) applies PERF[g_perf] -> sets col, bud, dd, dc, mmin, flod, clod in one shot. HUD label pf=PERFNM[g_perf] (lo/md/hi/ul). Off-grid (reachable via P-cycle as a preset, label is a string not a number). | 0..3 (case 26: +4+dd %4) -> lo/md/hi/ul. PERF[]={lo:dd450/mn200, md:600/500, hi:800/800, ul:1000/off} (line 399). Default g_perf=1 (md). Reset (case 27) sets g_perf=1 then overrides several dials. | META -- itself free, but it REWRITES 7 perf dials at once, so the perf effect is whatever the chosen preset's col/bud/dd/dc/mmin/flod/clod imply. lo = cheapest (low col, low bud, short dd); ul = most expensive (col80, bud, dd1000, murk off). | No direct pixels; it snaps the whole quality/perf tradeoff. Higher preset = denser columns, bigger draw budget, farther draw distance, finer flats (and the cfgflash shows the name ~2s, line 1801). | Master over col (1), bud (13), dd (0), dc (1/g_dci), mmin (8), flod (11), clod (16) -- toggling pf clobbers all of them. Does NOT touch vmap (vmap is kept independent, line 1578). dd/mmin indices track DD[]/MURKMIN[] (line 399). Overlaps in scope with rset (param 27) which is a full hard reset rather than a preset pick. |
| 26 | `pf` | Off-grid perf-preset selector: applies one of 4 named dial-vector presets (lo/md/hi/ul) from PERF[4] (line 399). Case 26 steps g_perf and immediately APPLIES the row, overwriting col/bud/dd/dc/mmin/flod/clod (line 1763). The fuller preset application at line 1575-1578 also sets nclp/gen/zon/ncul/bclp/occl/skip/hmap/cap/rad/seam/hwt. HUD shows PERFNM[g_perf]. | 4 presets, name shown via PERFNM (lo/md/hi/ul). Default index 1 (md). Case 26 steps by dd wrapping mod 4. PERF rows (line 399): lo=dd450/mn200, md=600/500, hi=800/800, ul=1000/off. | META - it sets MANY hot-path dials at once (dd, bud, col, dc, mmin, flod, clod and more). Cost is whatever the chosen row implies: lo = cheapest (short dd, tight murk, low budget), ul = dearest (dd1000, murk off). It is the bulk perf lever; pick a lower preset to speed up everything. | Wholesale quality/range change: lo pulls the horizon in and thins strips/flats; ul draws far with murk off. Affects draw distance, fog, strip budget, column width and LOD together. | OVERWRITES nearly every other perf dial it touches - notably it drives hmap (param 38) and hwt (param 30), col (param 1), bud (param 13), dd (param 0), dc (param 1->dc), mmin (param 8), flod/clod (params 11/16), nclp (param 36), gen/zon/ncul/bclp/occl/skip/cap/rad/seam. It does NOT set vmap (param 24) - the comment notes vmap=2 is to be kept fixed (line 1578). Reset (case 27) sets g_perf=1 (md). |
| 27 | `rset` | RESET to defaults: a one-shot action (case 27, line 1764) that writes ~28 dials at once back to the canonical baseline -- dd14(750)/dc6(7)/col0/mmin5/bud26(336)/fog14/flod0/clod0/fmrk0/cmrk0/ease16/zon0/gen1/occl1/cap0/sky1/seam0/rad0/hclip0/lutcull0/hwt1/perf1/ncull1/skip1/hmap3/vmap3/nclp2(16)/bspviz0/bandclip1/dpri1/actdd3(200)/maxact8. HUD label just 'rset'. Off-grid (action, no value). | Action only (no state). Pressing it restores the full default dial set. The label tk[27] is literally "rset". | One-shot write; its perf effect is the resulting default config (bud336, hmap3/vmap3 perspective, far-cull+skip+band-reject ON, dpri burst). After reset, perf is the mature default profile. | Snaps every render dial back to the shipping defaults in one keypress -- the baseline restore after experimenting. | Touches essentially EVERY documented param (0-42). Note it sets g_budgeti TWICE (8 then 26 -> 336 wins) and reasserts hmap=vmap=3 (the shared perspective divide), dpri=1, ncull/skip/bandclip on, actor-dd=200/maxact=8. Distinct from pf (param 26): rset is a hard full restore, pf is a 7-dial preset bundle. |
| 27 | `rset` | Off-grid one-shot RESET to defaults: case 27 (line 1764) writes the whole dial vector in one go - dd=14(750)/dc=6(7)/ncoli=0/murkmin=5/budget=26(336)/fogext=14/flod=0/clod=0/vpw=0/vph=0/murkease=16/zonal=0/generic=1/occl=1/capmode=0/opensky=1/seamover=0/radial=0/hclip=0/lutcull=0/hwtail=1/perf=1/ncull=1/skip=1/hmap=3/vmap=3/nclip=2(16)/bspviz=0/bandclip=1/dpri=1/actddi=3(200)/maxact=8. | Action, not a value (HUD label is just `rset`). No stored state. Triggered by adjusting param 27. Summary: dd750/mn250(via mmin idx)/dc7/nclp16, hmap3/vmap3 perspective-correct, far-cull+skip+band-reject ON, actor-dd=200/maxact 8, bud336. | NONE itself (a one-time write of ~30 globals). It forces nothing expensive, but the resulting STATE (dd750, bud336, ncull/skip/dpri ON, hmap3/vmap3) defines the default cost baseline. | Snaps the entire renderer back to the shipped/known-good look in one action - perspective-correct walls and floors (hmap3/vmap3), playable draw distance, far-cull and static-skip on. Use it to return from an experimental dial configuration. | Touches almost every documented param: it overrides perf presets (sets g_perf=1 but then hard-sets the individual dials too), forces hmap3+vmap3 (the shared-perspective-divide pair), dpri+bandclip+ncull+skip+hwt ON, and resets actor dials add(idx3)/mxa(8). It deliberately sets bud twice (idx ...then 26) - the final g_budgeti=26 (=336) wins. Does NOT reset g_hidehud (param 21) or the pure debug views untx/fdbg/bspv/nemt. |
| 28 | `vprj` | PER-VERTEX PROJECTION CACHE (g_vproj, repurposed from the dead hclip flat-clip). In vs_render_seg: when a seg's v1 equals the PREVIOUS seg's v2 (ve_x0==g_vcx && ve_y0==g_vcy, the subsector-loop common case, line 899) it reuses the cached raw projection (depth ad, lateral aS) and SKIPS the 4 v1 rotation MULs. Caches each seg's v2 projection for the next seg (line 903). | Boolean 0/1 (case 28: !g_vproj). Default 1 (on, line 893). Reset (case 27) leaves it -- not in the case-27 list, so it persists. (Note: tk[28] reads g_vproj; the old g_hclip param-28 case was retired.) | PROJECTION (per-seg) -- this IS a perf optimisation. ON saves 4 MULs per seg whose v1 was the previous seg's v2 (very common inside a subsector fan). Leaving it ON speeds up the projection stage; OFF is the A/B baseline that always re-projects v1. | NONE visual -- the cached projection is identical to recomputing it. Purely a speed toggle (A/B that the cache is correct/worth it). | Operates on the same per-seg projection as vmap/hmap perspective (shares vs_render_seg's ad/aS/bd/bS). Independent of, and stacks with, the occlusion pre-reject (#49, line 917) and near-clip (param 36). g_vcx/g_vcy/g_vcd/g_vcs reset each frame (camera-dependent). Repurposed slot -- the legacy g_hclip (height-clip flats) still exists as a variable (default 0, lines 848/881) but is no longer the param-28 case. |
| 29 | `lcul` | LUT-CULL (g_lutcull): PARK a floor/ceiling LUT block (one of the 40 flat sprites) when the nearest wall fully COVERS its 16px band -- the wall sprite (slot 41+) would overdraw it anyway. In vs_lut it scans the block's span against vs_clY (ceiling, line 841) / vs_flY (floor, line 871); if every covered render-col's wall edge covers the band, the block is parked instead of drawn. | Boolean 0/1 (case 29: !g_lutcull). Default 0 (OFF). Reset (case 27) sets 0. | FLATS (vs_lut). Intended to SAVE flat-sprite writes, but it ADDS a per-block span scan (g_colrcp map + loop over covered cols) up front. Measured NO net speedup on the ride (the fully-covered case is too rare to pay for the check) -> kept as a toggle. ON can be slightly slower in open views, marginally faster only where near walls blanket whole bands. | Pixel-IDENTICAL output (walls 41+ overdraw the LUT 1..40 either way). Pure perf A/B with no visual delta -- it just avoids drawing flat blocks that a wall hides. | Reads the same vs_clY/vs_flY near-wall clip arrays as hclp (the dead height-clip) and uses g_colrcp like the rest of vs_lut. Orthogonal to clod/flod (LOD row counts), fmrk/cmrk (murk shades), zon/gen (flat source). Its block->column mapping mirrors vs_lut's centre-sampling (sx+8). Net effect depends on bud/dd density (how often a near wall covers a whole band). |
| 30 | `hwt` | SCB tail-clear strategy when a strip shrinks vertically frame-to-frame. 1 = high-water: blank only the stale SCB delta past the per-slot high-water mark 'cot' (g_scb_hw[slot]); 0 = the old behaviour that pads every shrunk strip back out to a fixed 16 tiles. Read in vs_strip_emit (line 615: `int hw=g_hwtail?g_scb_hw[slot]:16;`). | Boolean toggle 0/1. Default 1 (ON). Toggling does NOT force a redraw. Also driven by perf presets (PERF[].hwt, applied at line 1578). | EMIT (SCB strip writes). On=1 writes fewer trailing blank SCB rows per shrunk strip (only the delta past the high-water), so it is the perf-positive setting; off=0 always pads to 16 = more VRAM writes per shrinking strip. The toggle exists to A/B-prove the high-water bookkeeping is not a net loss. Speeds up: ON. | Cosmetic only when stable; the difference shows as garbage/stale tile trails on strips that are mid-shrink if the tail isn't cleared correctly. Correct frames look identical either way. | Lives in the SCB emit path alongside the dpri burst (it clears the tail of whatever vs_strip_emit writes). Independent of WALK/PROJECTION dials. Reset (case 27) forces it to 1; perf presets (param 26) overwrite it from PERF[].hwt. |
| 31 | `untx` | Forensic sprite-budget view: every wall strip is drawn with the uniform PLACEHOLDER_TILE instead of its real texture, so the sprite GRID is visible (T set at line 716; palette is a 2x2 col/row checker at line 721: `TEXBASE+((c&1)\|((r&1)<<1))`). Lets you count strips across (vertical seams), each strip's tile-height, and the hardware vertical V-shrink. | Boolean toggle 0/1. Default 0 (OFF). No redraw forced on toggle. | NONE / cosmetic. Same number of strips emitted and same SCB writes; only the tile index + palette per strip change. No effect on WALK/EMIT/FLATS/PROJECTION budget. Pair with the HUD ns=/t= counts to read the grid. | All walls become flat checkerboard placeholder tiles; textures vanish. Reveals the strip-grid geometry and per-strip vertical squash from hardware scaling. | Shares the placeholder-tile path with bspv (param 37): line 716 keys on `g_untex\|\|g_bspviz`, and line 721 picks bspv's depth-band colour OVER untx's checker when both are on, so bspv wins. Pure debug view; ignored by perf presets and reset. |
| 32 | `fdbg` | Forensic flat-source view: each floor/ceiling patch is painted a SOLID colour keyed by its flat ID (TEXBASE+(fid&7)) using PLACEHOLDER_TILE, so you can see which sector's flat draws where (the pit-blue-on-main-floor overspill). Ceiling at line 853, floor at line 886; also stamps the centre-column's picked front/back floor flats for the HUD (line 993: g_dbg_ffl/g_dbg_bfl). | Boolean toggle 0/1. Default 0 (OFF). No redraw forced on toggle. | NONE / cosmetic. It only swaps the flat tile + palette inside the existing floor/ceiling fill (FLATS / vs_lut path); no extra rows or strips. Does not change the walk or emit counts. | Floors and ceilings become solid ID-coloured blocks instead of textured flats; makes mis-assigned sectors (e.g. pit flat bleeding onto the main floor) immediately visible. | Reads the zonal/generic flat selection (line 853/886 branch on g_zonal && !g_generic) so what it colours depends on params 4 (zon) and 5 (gen). Centre-column readout (line 993) feeds the HUD bn/flat line. Pure debug; untouched by presets/reset. |
| 33 | `bnch` | On-demand per-stage timing burst. Toggling ON requests ONE 16x render burst at the current view that decomposes the frame into FULL / emit / flats / proj stages (lines 1828-1835), freezing the breakdown on the HUD bn line (1863). The only timer is g_vbl (vblank-granular) so a single frame's stage delta is unmeasurable; it runs each config 16x and reads accumulated vblanks. | Tri-state 0/1/2. Case 33 toggles between 0 and 1 (`g_bench=g_bench?0:1`); the render loop sets it to 2 (done/showing) after the burst completes. 1 = run next frame, 2 = frozen breakdown shown, 0 = cleared. Default 0. | HEAVY but ONE-SHOT: when armed (=1) it runs vs_render ~16x per stage at the current view (a ~1-2s hitch), then freezes (=2) and costs nothing until re-armed. It is a measurement tool, not a steady-state dial. A periodic auto-burst caused the earlier large stall, so this is request-only. | No visual change to the scene; surfaces the bn fa=/em=/fl=/pj= timing line on the HUD (16x-vblank units; lower is cheaper) so you can see which stage dominates as you crank other dials. | Internally drives g_noemit (param 43 / g_noemit) and overrides g_vs_budget/g_floor_rows/g_ceil_rows for its sub-measurements (lines 1823/1833). It is the readout for the cost of every other param. Reset (case 27) does not touch it; not in presets. |
| 34 | `skip` | Static-frame skip: reuse last frame's SCB when nothing visible changed (camera pos/angle/anim phase/doors/lifts/fire/fx all static). The stat test is at line 1796 and only renders when !stat (line 1800); idle frames increment g_skipcnt instead. g_drewactors gates the anim-phase wake so empty rooms skip 100%. | Boolean toggle 0/1. Default 1 (ON). Case 34 toggles and forces g_redraw=1 so the view is fresh on toggle. Also set by perf presets (PERF[].skip, line 1577). | WALK + EMIT + FLATS - the biggest single perf win when idle: ON makes a static frame cost ~0 (no vs_render at all). OFF re-renders every frame unconditionally. Speeds up: ON (only helps when the view is actually static; no benefit while moving). Watch skp= on the HUD climb ~60/s while idle to confirm it fires. | No visual change while static (the SCB persists); on a real change the frame renders normally. Gun/HUD are fix-layer so they persist across skipped frames. | Disabled implicitly by bspv (param 37): the stat test at line 1796 includes `!g_bspviz`, and bspv forces a redraw each frame. g_redraw (set by dial changes, map switch, kills) overrides it for one frame. Reset (case 27) -> 1. Mutually fine with all render dials; it just gates whether they run. |
| 35 | `ncul` | Node far-cull: prune a BSP subtree wholly beyond the far-horizon (g_murk_eff) in vs_bbox_vis so dd/murk limit the WALK (projection), not just the strip emit. Cheap min-depth test at line 1044 (2 MULs to the nearest corner) and the prune at line 1063 (`if(g_ncull && dmin>g_murk_eff) return 0`). | Boolean toggle 0/1. Default 1 (ON). Case 35 forces g_redraw=1. Set by perf presets (PERF[].ncul, line 1577). | WALK / bbox (the bx node tests + pj). ON prunes whole far subtrees before the 4-corner projection (~22 MULs saved per pruned box), cutting the BSP walk and projection cost. Most effective at playable dd / dense views where the murk pulls the horizon in; a no-op at dd=32000 (nothing is beyond the map edge). Speeds up: ON. | Removes far geometry past the fog horizon; the open columns then show the floor/ceiling LUT + murk backdrop (the beyond-fog look). Off = far floors/ceilings can extend past the fog. Safe: only prunes a box whose NEAREST corner is past dd, so nothing visible (emitted strips are already capped at g_murk_eff, line 977) is lost. | Gated by the far horizon = g_murk_eff, which is set by dd (param 0) and mmin/murk (param 8); ncul does nothing if those put the horizon at the map edge. Pairs with radial (param 23) which culls per-column. Complements the strip-emit far-cull at line 977. Reset -> 1. |
| 36 | `nclp` | Near-clip distance dial: index into NCLIP[] (line 515), sets g_vs_near = NCLIP[g_nclip] (line 1787) which replaces VS_NEAR for the WALLS and the BSP-walk bbox. Pushes the near plane out to clip huge near walls. Index 0 is degenerate (near vtx project to centre + back-face cull, safe). | Index 0..17 into NCLIP[]={0,8,16,24,32,40,48,56,64,80,96,112,128,144,160,176,192,200} world units (NNCLIP=18). HUD shows the resolved value (NCLIP[g_nclip]). Default index 2 = 16. Case 36 steps by dd (the B/N delta) wrapping mod NNCLIP. Also driven by perf presets (P->nclp, line 1575). | WALK + PROJECTION + EMIT. A larger near-clip discards the widest, most screen-eccentric near strips before projection/emit (perf probe: do the huge near walls dominate?). Bigger value = fewer/cheaper near columns but more aggressive near clipping. It is a probe, not a recommended high value. Speeds up: larger value (at a visual cost). | Pushes the near plane outward, clipping walls closer than the value to nothing - very large values eat the wall right in front of you. Default 16 keeps near walls intact. | Only the WALLS + BSP bbox use g_vs_near; actors and shooting keep the fixed VS_NEAR (line 516) so aim/gameplay stay stable regardless. Set by perf presets (param 26) from P->nclp. Reset (case 27) -> index 2. |
| 37 | `bspv` | BSP traversal viz (off-grid): walls drawn as solid DEPTH-coloured strips (8 bands near->far, bspb=d>>6 capped at 7, line 714) REVEALED progressively front-to-back in BSP-walk order. g_bspstep limits how many strips emit this frame (line 1306) and ramps +2/frame, looping at 320 (line 1793), so you watch the live BSP sweep. | Boolean toggle 0/1. Default 0 (OFF). Case 37 toggles, resets g_bspstep=0 and forces g_redraw=1. The reveal count g_bspstep auto-ramps each frame while ON. | Cosmetic per-strip (same placeholder-tile path as untx, no extra projection) BUT it forces a full redraw every frame (overrides static-skip, line 1796) and ramps the emit count, so it is NOT a perf-neutral steady state - it defeats the idle-skip win while running. Pure diagnostic. | Walls become depth-banded solid colours that paint on front-to-back over successive frames, so you literally watch the BSP walk order and the near->far depth banding. Loops the sweep. | Shares the PLACEHOLDER_TILE path with untx (param 31): at line 716/721 when both are on, bspv's depth-band colour (TEXBASE+bspb) wins over untx's checker. Forces redraw -> defeats skip (param 34) and the stat test excludes it. The g_bspstep gate also caps the dpri burst emit (line 1306). Reset -> 0. |
| 38 | `hmap` | Horizontal wall-U mapping mode. 0 = screen-mapped (tc=c) everywhere; 1 = affine wall-U everywhere (glued but swims on tiling walls); 2 = SELECTIVE (affine only for single-copy posters/signs via `persp` at line 924, screen-mapped for tiling walls); 3 = PERSPECTIVE-CORRECT wall-U everywhere via a 1/z-linear world-fraction interp (line 980-981). Computes tc (texture column). | Mode 0..3 (`g_hmap=(g_hmap+1)%4`, case 38, forces redraw). Default 3 (default per line 501; comment notes mode 2 is also acceptable). Note: hmap is preset-driven (PERF[].hmap, line 1578) so param 26 overrides it; vmap is NOT. | PROJECTION (per emitted column). Mode 3 costs ~1 32-bit divide per emitted column for the 1/z-linear world fraction `wf` (line 980 `den`/divide). Modes 0-2 avoid that divide (0 is free screen-map, 1/2 are affine shifts). The divide runs ONLY on emitted columns (not every walked one - that was the 1fps regression). Speeds up: lower mode (0 cheapest, 3 dearest). | 0 = textures slide with the screen (sign split/drift/duplication on angled walls); 1 = glued but affine-swims; 2 = posters glued, tiling walls screen-mapped; 3 = all walls glued AND swim-free (textures stay locked to the geometry). | hmap3 and vmap3 (param 24) SHARE the perspective divide: line 980 computes `wf` once `if(g_hmap==3 \|\| g_vmap==3)`, then hmap3 uses it for tc and vmap3 for depth - so running both 3 costs one divide, not two. Mode 2 reuses the `persp`/`isposter` selection (line 924) with mode 1's affine path. Driven by perf presets; reset -> 3. |
| 39 | `bclp` | Vertical band-reject: skip a seg whose projected vertical span misses every open column's surviving sliver - the near-window slowdown fix. In vs_render_seg (lines 938-940) it computes the seg's top/bottom (segt/segb) and scans columns ca..cb; if no open column [vs_ct,vs_cb] overlaps the seg band it returns early and increments g_seg_clipped. | Boolean toggle 0/1. Default 1 (ON). Case 39 toggles (no forced redraw). Set by perf presets (PERF[].bclp, line 1577). | WALK / per-seg projection (it is a pre-reject BEFORE the full per-column projection at line 941). ON adds a cheap ca..cb scan but skips the expensive full projection for segs that contribute nothing to the open window - net win in near-window views where many segs are vertically occluded. Speeds up: ON (in tall-occlusion / window views). Watch bc= on the HUD (segs killed this frame). | Cosmetic when correct (rejected segs were invisible anyway). If mis-tuned it can pop window edges / cause segs to flicker at the band boundary - the comment says watch sg/bc + window edges for popping when A/B-ing. | Runs after the #49 span-vs-open occlusion pre-reject (the sa/sb/ca/cb are computed up-front for both). Operates on the same vs_ct/vs_cb open-column arrays that radial (param 23) and the walk close. Feeds the HUD bc counter (line 1862). Set by perf presets (param 26); reset -> 1. |
| 40 | `dpri` | Depth-prioritised budget: instead of halting the BSP walk when the strip budget fills, RECORD every strip within the horizon into g_sbuf (up to VS_SBUFN=512), then post-walk keep the NEAREST g_vs_budget strips by depth via a 32-unit-bucket depth histogram (lines 1294-1310). Off = old walk-order halt (stop recording at the budget). Changes the record-cap branches at lines 897 and 977. | Boolean toggle 0/1. Default 1 (ON). Case 40 forces g_redraw=1. NOT preset-driven by name in the loop (set by reset to 1). | WALK vs EMIT trade. ON records to the 512-slot buffer (deeper walk, then a cheap histogram pass + a re-pack emit) so the walk runs further but the EMIT count is still capped at g_vs_budget (SCB emit hard-capped at slot 380). OFF halts the walk at the budget (shorter walk) but emits in BSP visit order. ON costs a bit more walk for correct near-wall priority; the burst itself is cheap (64-bucket histogram). Speeds up: OFF reduces the walk; ON is the quality default. | ON guarantees the nearest g_vs_budget strips are the ones drawn, so near walls never drop out when the BSP visits them after the budget fills (fixes the dense-courtyard near-wall flicker). OFF can drop near walls that the walk reaches late. | Governs how bud (param 13 / g_vs_budget) is enforced: with dpri ON, bud is a depth threshold applied post-walk; OFF, bud halts the walk. The record cap reads VS_SBUFN when dpri else g_vs_budget (lines 897/977). The kept (emitted) count then feeds the actor budget-halt leak fix (line 1316). The bspv reveal gate also clamps the burst emit (line 1306). Reset -> 1. |
| 41 | `add` | Actor draw distance: index into the shared DD[] array (line 369), pulls monsters/props in CLOSER than the wall horizon independently of the wall dd. At line 1317 `if(DD[g_actddi] < acull) acull=DD[g_actddi]` clamps the actor cull distance - the open-arena lever (E2M5) where occlusion can't help. | Index 0..32 into DD[]={0,100,150,200,250,...,32000} world units (VS_NDD=33). HUD shows the resolved value DD[g_actddi]. Default index 3 = 200. Case 41 steps by dd wrapping mod VS_NDD and forces redraw. Set toward 32000 = effectively off (actors never far-culled). | WALK/sort over THINGS (the actor loop at 1318+), not the wall path. A smaller value culls distant actors earlier -> fewer enter the nearest-N sort and fewer sprite/scanline-budget draws (perf-positive in monster-heavy open views). Larger value shows more distant actors at a cost. Speeds up: smaller value. | Controls how far monsters/props/billboards remain visible. Low = actors fade out close (denser-map perf); high = distant actors stay drawn. Independent of where walls/floors fade. | Reads the SAME DD[] table as the wall dd (param 0), but a separate index (g_actddi) so actors cull independently of walls. Combined at line 1315-1317 with g_murk_eff/g_vs_murk and the budget-halt leak fix (line 1316). Feeds the nearest-N sort bounded by mxa (param 42). Reset (case 27) -> index 3 (=200). |
| 42 | `mxa` | Runtime nearest-N actor cap: draw at most the nearest g_maxact actors this view. Bounds the depth-sort insert (lines 1339-1340, an insertion sort into bd[]/bi[] kept DESC) and protects the sprite/scanline budget; the rest fog out. Array storage is MAXACT=32 (line 412); g_maxact is the live cap. | Integer 1..MAXACT(32). Default 8. Case 42 steps by `dd*2` (the B/N delta doubled), clamps to [1,MAXACT], forces redraw. Dial UP for more simultaneous actors, DOWN for monster-heavy-view perf. | WALK/sort + EMIT over THINGS. A smaller cap shrinks the nearest-N insertion sort and, critically, the number of actor sprites emitted to SCB / scanlines (perf-positive in crowded views). Larger cap = more visible enemies but more sprite-strip emit and scanline pressure. Speeds up: smaller value. | Number of simultaneously drawn actors. Low = only the nearest few enemies/props render (others vanish even if in range); high = up to 32 at once. Visual density of monsters. | Hard-bounded by MAXACT=32 (the bd[]/bi[] array size, line 1314) - cannot exceed it. Works WITH add (param 41): add sets HOW FAR actors are considered, mxa sets HOW MANY of the nearest survive. The selected/emitted counts feed the HUD act=g_act_emit/g_act_sel line (1862). Reset -> 8. |
| 43 | `nemt` | Forensic no-emit: run the full BSP walk + projection + clipping (early-outs intact) but SKIP the vs_strip SCB writes, isolating wall projection from emit. Gated at the strip-emit visibility test (line 977 `vis = vs_emit && !g_noemit && ...`) so emitted columns are computed but not written. | Boolean toggle 0/1. Default 0 (OFF). Case 43 toggles and forces g_redraw=1. Also driven internally by the bnch (param 33) sub-measurements (lines 1823/1833 set g_noemit=1 around the proj-only bursts). | DIAGNOSTIC - it REMOVES the EMIT (SCB writes) stage while keeping WALK + PROJECTION + FLATS. Toggle live: if fps JUMPS the frame was EMIT-bound (the VRAM-write floor); if fps is UNCHANGED it's walk/overhead-bound. So it doesn't 'speed up' as a setting - it attributes cost. ON = no walls drawn. | Walls disappear (no SCB strips written) while the rest of the pipeline still runs - floors/ceilings/actors may still draw. Purely to isolate emit cost; not a viewing mode. | It is the lever behind bnch's em= measurement: bnch sets g_noemit to split FULL vs proj-only (lines 1823/1833). Paired with bud/dpri (params 13/40) for the record-vs-emit story - it shows the emit half of that trade. Reset (case 27) -> 0. |

## Verify corrections
- p7 `name/values/effect/definition (weapon count)`: WRONG COUNT. The row says "6 first-person weapon sprites" and "0..GUNHAND_N-1 ... 0=fist .. 5=chainsaw". GUNHAND_N is actually 8 (neogeo/gunhand.h:2). The GUNHAND[] table (gunhand.h:9-18) holds 8 weapons: 0=FIST,1=PISTOL,2=SHOTGUN,3=CHAINGUN,4=ROCKET,5=CHAINSAW,6=PLASMA,7=BFG. case 7 (main.c:1739) cycles g_weapon over 0..7 ( (g_weapon+GUNHAND_N+dd)%GUNHAND_N ). So the dial spans 8 weapons, not 6, and chainsaw is index 5 of 0..7 (not the last). The rest of row 7 (default g_weapon=1 pistol at main.c:385, g_pweap last-drawn, cosmetic fix-layer redraw on change, not touched by rset at main.c:1764) is correct.
- p13 `values (pf preset budgets hi/ul)`: WRONG RESOLVED VALUES for the high/ultra presets. Row says "pf presets set it (lo=48,md=96,hi=192,ul=276)". PERF[] bud indices are {2,6,12,22} (main.c:399, perfpreset_t field order col,bud,dd,dc,mmin,flod,clod). BUDGET[2]=48 (lo OK), BUDGET[6]=96 (md OK), BUDGET[12]=168 (hi is 168, NOT 192), BUDGET[22]=288 (ul is 288, NOT 276). Everything else in row 13 verified: default g_budgeti=26->BUDGET[26]=336 (main.c:482), case 13 wraps +/-1 (main.c:1746), record-cap/walk-halt at main.c:897 & 977, depth path at main.c:1294-1310 (64-bucket histogram, keep nearest g_vs_budget by depth), budget-halt bounds far actors at main.c:1316, dpri(40) mode switch, rset->26 (main.c:1764).
- p11 `interactions (pf preset framing)`: MISLEADING preset framing. Row says "pf presets crop it (lo=6,md=3,hi/ul=0)". The numbers 6,3,0,0 are the literal PERF[] flod INDICES (main.c:399), which is consistent with the row stating indices elsewhere, BUT FCLOD_F={5,5,4,4,3,3,2,2,1,0} (main.c:418) means index0 = FULL 5 rows (NO crop) and index6 = 2 rows (heaviest crop). So the resolved row counts are lo=2, md=4, hi/ul=5(full) -- i.e. hi/ul do NOT crop the floor at all; lo crops it most. The phrase "crop it ... hi/ul=0" reads backwards (higher preset = LESS crop). The rest of row 11 is correct: FCLOD_F values, idx0=full/idx9=off, default g_floorlod=0->5 rows, case 11 wraps (main.c:1744), g_floor_rows is the LUT draw-loop row count (main.c:828,880), bench harness forces 4/6 rows (main.c:1820-1822).
- p0 `cost ("per-vertex cull (line 1046)")`: MINOR MISLABEL. Row calls main.c:1046 "the per-vertex cull". Line 1046 is NOT per-vertex; it is the node far-cull's CHEAP min-corner pre-check inside vs_bbox_vis's if(g_ncull) block (computes the bbox's single nearest-corner view-depth, 2 MULs, and prunes if > g_murk_eff). It is the same g_ncull node-cull mechanism as line 1063 (the full 4-corner test), just an early-out. There is no separate per-vertex dd cull. The substance of the row (dd prunes the WALK only via ncul at 1046/1063, otherwise only gates emit at 977) is correct; only the "per-vertex" label is inaccurate. All other dd claims verified: DD[] 33 entries, default g_dd=14->DD[14]=750, HUD shows g_vs_murk (main.c:1839), clamp order dd-last (main.c:1293), actors cull at min(g_murk_eff,dd) (main.c:1315), pf dd indices {8,11,15,19}=450/600/800/1000, rset->750.
- p18 `effect/interactions (gen-gating claim)`: WRONG: the doc says fmrk 'only applies to the SYNTHETIC floor plane (gen=1 ...), not real flats' and 'Effective only when gen (param 5) selects the synthetic plane.' Verify against the actual floor loop at main.c line 883: the murk palette override `fp=(r<fskip+g_vpw)?...:fpal` is written UNCONDITIONALLY per row, with NO test on g_generic. It is the very NEXT line (884) that conditionally overwrites fp: `if(g_zonal && !g_generic) fb=vs_flatres(...,&fp)`. So the real gating is: murk applies whenever the ZONAL path is OFF -> i.e. gen=1 OR (gen=0 AND zon=0). It is DEFEATED only by the zonal real-flat lookup (zon>=1 && gen=0), not by gen alone. The correct statement: 'murk applies to the synthetic plane (gen=1) AND to the blanket real-flat path (gen=0, zon=0); only the per-row ZONAL stamp (zon>=1, gen=0) overwrites it.' Everything else in this row is correct: decl line 408 (g_vpw, default 0), case 18 `(g_vpw+NVP+dd)%NVP` with NVP=6 (line 1751), reset sets 0 (line 1764), palette band 11->10->14 (line 883), FLATS-trivial cost.
- p19 `effect/interactions (gen-gating claim)`: WRONG, same defect as id 18: 'Synthetic ceiling plane only (gen=1)' / 'Effective only with gen (param 5)' is not what the code does. main.c line 850 writes the murk palette `cp` per row UNCONDITIONALLY (no g_generic test); line 851 `if(g_zonal && !g_generic) ... vs_flatres(...,&cp)` is what overwrites it. So cmrk applies to real flats too whenever zon=0; it is only overridden on the ZONAL path (zon>=1 && gen=0). Correct rest: decl line 408 (g_vph), case 19 `(g_vph+NVP+dd)%NVP` NVP=6 (line 1752), reset 0 (1764), ceiling band palette 9->15->14 (line 850), FLATS-trivial cost.
- p25 `definition/values/interactions (no-B/N-adjust claim)`: WRONG: the row says 'case 25 is NOT in the param switch ... map switching is driven elsewhere (g_map), this is the read-out' and 'No B/N adjust case in the documented switch range.' True that there is no explicit `case 25:`, but the switch's `default:` (main.c line 1777) catches NSEL==25 and DOES adjust the map on B/N: `default:g_map=(g_map+VE_NMAP+dd)%VE_NMAP; vs_set_map(g_map); ... SND(0x10+MUSIC[g_map]);`. So lvl IS the B/N map selector (cycles maps + swaps soundtrack), not merely a passive read-out. The tk[25] HUD label (`lvl=%d`,g_map+1) at line 1851 is correct, and the per-map reload cost note is correct, but the 'driven elsewhere / no adjust' framing is wrong: NSEL=25 with B/N is exactly what switches the level.
- p24 `definition (line attribution)`: Minor location imprecision (substance is correct): the row says the companion/selective mode is 'Selected in vs_render_seg per-seg (line 707)'. Line 707 is inside vs_strip_emit (the `g_vmap==1\|\|g_vmap==3\|\|(g_vmap==2&&poster)` companion-map branch), NOT vs_render_seg. The per-seg isposter classification that drives mode-2 IS in vs_render_seg (line 923, `ve_ulen[s]<=TEXWT[mt]`). All values/defaults/cost are right: g_vmap=3 default (line 493, and the row correctly flags the trailing 'Default 2' decl comment as stale), case 24 `(g_vmap+1)%4` forces g_redraw=1 (line 1762), NOT preset-driven (line 1578), reset sets 3 (line 1764), mode-3 shares the 1/z divide with hmap=3 (line 980 `if(g_hmap==3\|\|g_vmap==3)`).
- p26 `definition / interactions / values`: MAJOR: row 26 conflates two SEPARATE preset systems. Param 26 (pf) at case 26 line 1764 ONLY applies PERF[g_perf] (a perfpreset_t with exactly 7 fields: col,bud,dd,dc,mmin,flod,clod -- struct decl line 398). It does NOT touch nclp/gen/zon/ncul/bclp/occl/skip/hmap/vmap/cap/rad/seam/hwt. The 'fuller preset application at line 1575-1578' the row attributes to pf is actually cfg_apply() (lines 1574-1579) reading CFGP[] (cfgpreset_t, 23 fields, decl line 1561), a DIFFERENT control triggered by the GAMEPLAY P button (line 1722, when !g_dbg cycles g_cfgsel through CFGP[]), not by param 26. So the interactions claim that pf 'drives hmap (param 38) and hwt (param 30)... nclp, gen/zon/ncull/bclp/occl/skip/cap/rad/seam' is WRONG -- those are cfg_apply/CFGP, not pf. Param 26's actual application line is 1764 (row says 1763, off by one). The parenthetical 'comment notes vmap=2 is to be kept fixed (line 1578)' is also wrong: the line-1578 comment says 'vmap now preset-driven too' and cfg_apply DOES set g_vmap (CFGP[].vmap). The narrow claim 'param 26 does not set vmap' is true for param 26 itself.
- p27 `interactions`: WRONG: row says reset 'Does NOT reset ... the pure debug views untx/fdbg/bspv/nemt.' Reset (case 27, line 1764) DOES explicitly set g_bspviz=0. (It correctly leaves g_untex, g_flatdbg, g_noemit, g_hidehud untouched.) This also contradicts row 37 which correctly states 'Reset -> 0' for bspv. All other reset values verified correct (dd=14/DD=750, dci=6/DC=7, murkmin=5/250, final budgeti=26/336, hmap=3, vmap=3, nclip=2/16, hwtail=1, ncull=1, skip=1, bandclip=1, dpri=1, actddi=3/200, maxact=8).
- p30 `values / interactions`: WRONG preset attribution: 'Also driven by perf presets (PERF[].hwt, applied at line 1578)' and 'perf presets (param 26) overwrite it from PERF[].hwt.' PERF (perfpreset_t, line 398) has NO hwt field and param 26 (line 1764) never touches g_hwtail. g_hwtail is set only by cfg_apply (CFGP[].hwt, line 1578 -- the gameplay-P preset, NOT param 26) and by reset (line 1764). Also: 'Toggling does NOT force a redraw' is misleading -- case 30 (line 1769) doesn't set g_redraw itself, but the blanket 'if(dd) g_redraw=1' at line 1779 forces a redraw on ANY value change (dd=+-1 for a toggle). Minor: the g_hwtail read at line 615 is inside helper vs_scb_clear_tail (line 614), which is CALLED by vs_strip_emit (line 650) -- the row says the read is 'in vs_strip_emit (line 615)'; line 615 is correct but the enclosing function is vs_scb_clear_tail.
- p31 `values`: 'No redraw forced on toggle' is misleading. Case 31 (line 1770) does not set g_redraw itself, but the blanket 'if(dd) g_redraw=1' at line 1779 forces a redraw whenever the value actually changes (dd=+-1). The line refs (T at 716, checker palette at 721, bspv-wins-over-untx at 716/721) and 'ignored by perf presets and reset' are all correct.
- p32 `values`: 'No redraw forced on toggle' is misleading -- same as untx: case 32 (line 1771) sets no g_redraw, but line 1779 ('if(dd) g_redraw=1') forces a redraw on any change. All other claims verified: ceiling colour cp=TEXBASE+(cid&7) at line 853, floor fp=TEXBASE+(fid&7) at line 886, g_dbg_ffl/g_dbg_bfl centre-column stamp at line 993, branch on g_zonal&&!g_generic, var name g_flatdbg.
- p33 `cost / interactions`: Line attributions are off. The RUNTIME bnch block is lines 1829-1837 (row says stages at 1828-1835; the if(g_bench==1) opens at 1829). The override-of-budget/floor/ceil and g_noemit=1 lines the row cites as '1823/1833' are wrong: line 1823 is inside the COMPILE-TIME dev VS_BENCH block (frame-180, behind #if VS_BENCH), not the runtime bnch; the runtime g_noemit=1 sub-measurement is at line 1834 (line 1833 is the runtime baseline config that sets budget=0, no g_noemit). HUD bn line at 1863 and tri-state toggle (g_bench=g_bench?0:1 at 1772, ->2 at 1836) are correct.
- p34 `values`: WRONG preset attribution: 'Also set by perf presets (PERF[].skip, line 1577).' PERF (perfpreset_t, line 398) has no skip field; param 26 never sets g_skip. g_skip is set by cfg_apply (CFGP[].skip, line 1577 -- the gameplay-P preset system, NOT param 26) and by reset. Everything else verified: stat test at 1797 (row says 1796) includes !g_bspviz, !stat-renders at 1801 (row says 1800), case 34 forces g_redraw=1 (line 1773), reset->1.
- p35 `values / interactions`: WRONG preset attribution: 'Set by perf presets (PERF[].ncul, line 1577).' PERF has no ncul field; param 26 never touches g_ncull. g_ncull is set by cfg_apply (CFGP[].ncul, line 1577 -- gameplay-P, not param 26) and reset. Minor line refs: the cheap min-depth MUL test is at line 1046 (1044 is the comment/branch open); the prune is at line 1063 (correct). The 'emitted strips capped at g_murk_eff, line 977' citation is CORRECT (the in-code comment at line 1063 itself says '917', which is stale -- the row's 977 is right).
- p36 `values / interactions`: WRONG preset attribution: 'Also driven by perf presets (P->nclp, line 1575)' and 'Set by perf presets (param 26) from P->nclp.' g_nclip is set by cfg_apply (CFGP[].nclp, line 1575 -- the gameplay-P preset using a cfgpreset_t* P), NOT by param 26. PERF (perfpreset_t) has no nclp field; case 26 (line 1764) never sets g_nclip. Minor: g_vs_near=NCLIP[g_nclip] is at line 1788, not 1787 (1787 is the DC[] array). NCLIP[] array (line 515), NNCLIP=18, default idx2=16, case 36 step-by-dd-wrap (line 1775), actors/shoot keep VS_NEAR -- all correct.
- p38 `values / cost / interactions`: WRONG preset attribution (two errors): (1) 'hmap is preset-driven (PERF[].hmap, line 1578) so param 26 overrides it' -- PERF (perfpreset_t) has NO hmap field; param 26 (line 1764) never sets g_hmap. g_hmap is set by cfg_apply (CFGP[].hmap, line 1578 -- gameplay-P, not param 26). (2) 'vmap is NOT [preset-driven]' is WRONG -- line 1578 sets g_vmap=P->vmap (CFGP[].vmap); vmap IS preset-driven by cfg_apply. The default-3 claim is correct (g_hmap=3 init, line 501) though the line-501 comment text stale-says 'Default 2'. persp at 924, wf at 980-981, shared divide for hmap3/vmap3 at line 980, divide gated to emitted columns -- all correct.
- p39 `values / interactions`: WRONG preset attribution: 'Set by perf presets (PERF[].bclp, line 1577)' / 'Set by perf presets (param 26).' PERF has no bclp field; param 26 never sets g_bandclip. g_bandclip is set by cfg_apply (CFGP[].bclp, line 1577 -- gameplay-P, not param 26) and reset. Also 'no forced redraw' is misleading: case 39 (line 1756) sets no g_redraw, but line 1779 ('if(dd) g_redraw=1') forces a redraw on any change. Band-reject lines 938-940, g_seg_clipped, runs after the #49 horizontal occlusion pre-reject (line 917), feeds HUD bc at 1862 -- all correct.
- p43 `interactions`: Line attributions for bnch's internal g_noemit use are off (same class as row 33): the row cites 'lines 1823/1833 set g_noemit=1'. Line 1823 is inside the COMPILE-TIME dev VS_BENCH block (#if VS_BENCH, frame-180), not the runtime bnch. The runtime bnch sets g_noemit=1 at line 1834 (line 1833 is the budget=0 baseline config, no g_noemit). The definition (gated at line 977 'vis = vs_emit && !g_noemit && ...'), case 43 toggle+redraw (line 1760), and reset->0 are correct.
- p21 `values`: 'no forced redraw' is misleading. Case 21 (line 1755) sets no g_redraw, but the blanket 'if(dd) g_redraw=1' at line 1779 forces a 3D-view redraw on any toggle (dd=+-1); the status bar itself is fix-layer and handled by the once-on-toggle clear at lines 1803-1806 (row says 1803/1804/1806 -- correct). The remaining claims (g_hidehud line 405, cln/param 20 merges both at line 1754, draw_face gated on !g_hidehud at 1807, reset does not set g_hidehud) are correct.
- p40 `none`: Row 40 (dpri) verified correct: histogram lines 1297-1300 (32-unit buckets >>5, 64 entries), VS_SBUFN=512 (line 603), record-cap branches at 897 and 977 use (g_dpri?VS_SBUFN:g_vs_budget), SCB hard cap es>=339 ->slot 380 (line 1305), bspv gate at 1306, NOT preset-driven, case 40 forces redraw (line 1757), reset->1. No correction needed.
- p37 `none`: Row 37 (bspv) verified correct: bspb=d>>6 capped at 7 (line 714), placeholder path lines 716/721 with bspv winning over untx, g_bspstep gate at 1306, ramp +2/loop at 320 (line 1794), forces redraw and excluded from stat test (line 1797), case 37 resets g_bspstep=0 + redraw (line 1776), reset->0 (line 1764). No correction needed. (Note: this row's 'reset->0' is correct and directly contradicts row 27's erroneous claim that reset does not touch bspv.)
- p41 `none`: Row 41 (add) verified correct: DD[VS_NDD=33] (line 369), DD[3]=200 default (g_actddi=3 line 370), acull clamp at line 1317, case 41 step-by-dd-wrap + redraw (line 1758), combined with murk/budget-halt-leak-fix at 1315-1317, reset->idx3. No correction needed.
- p42 `definition`: Substantively correct; one minor line nit: 'Array storage is MAXACT=32 (line 412)' -- line 412 is the #define MAXACT 32, not the array; the bd[]/bi[] arrays are declared at line 1314 (which the row's own interactions section states correctly). Insertion sort DESC at 1339-1340, case 42 'g_maxact+=dd*2' clamp [1,MAXACT] + redraw (line 1759), reset->8 -- all correct.
