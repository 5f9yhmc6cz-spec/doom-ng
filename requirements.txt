# DOOM-NG asset pipeline (tools/*.py) -- Python package dependencies.
#
#   pip install -r requirements.txt
#
# These are the pip packages the asset bakers import. Everything ELSE the build
# needs is SYSTEM-level (install with your OS package manager -- see README.md):
#
#   - ngdevkit      Neo Geo toolchain (m68k-neogeo-elf-gcc, Z80 tools, tiletool.py,
#                   open BIOS, ngdevkit-gngeo)   https://github.com/dciabrin/ngdevkit
#   - a C compiler (gcc/clang) + make            -- builds the host baker (doomng-host)
#   - SDL2 (libsdl2-dev / sdl2)                  -- doomng-host window
#   - Python 3.11+   (the bakers AND ngdevkit's tiletool.py need it -- tiletool imports typing.Self)
#   - doom1.wad  (DOOM shareware -- bring your own; not redistributed here)
#   - ImageMagick (optional, the `magick` command -- for `make dump`)
#   - MAME (optional)                            -- cycle-accurate hardware validation

Pillow>=9        # tile/sprite quantisation + image I/O in the bakers (wad2c, *lut, gunbake, ...)
numpy            # buffer maths in wad2c and the dev profiling tools

# vizdoom        # OPTIONAL -- only tools/refcap.py (the reference frame-match dev harness)
